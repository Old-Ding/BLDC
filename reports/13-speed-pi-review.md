# 第 13 章三轮复核记录

## 高级工程师复核

- 核对控制链为速度误差、PI、duty 限幅、PWM、三相桥、电机响应，不把 PI 输出误写成转矩命令。
- 核对积分抗饱和只位于 `DerivativeFcn`，高限幅正误差和低限幅负误差时冻结，不在输出层重复判断。
- 核对四场景与 CSV、报告一致，最近一次结果为 `4/4 PASS`。
- 明确本章使用 Machine 实际速度反馈，第 14 章才接入 Hall 边沿测速。

## 高级 UI 工程师复核

- PLECS 原生 Scope 已修正高 DPI 裁切，完整显示 0-0.2 s 速度阶跃。
- MATLAB 图按正常调节、duty、抗饱和恢复和恢复 duty 排列，图例位于绘图区外侧。
- 正文先建立数据流，再给参数、场景、图和文件表；未把实验包放在教学内容之前。

## 高级心理学家复核

- 从“PI 不能直接命令速度”切入，降低读者把控制器输出误认为转矩的风险。
- 用相同输入、只切换抗饱和开关构造对照，读者可以独立判断因果。
- 明确失败场景 PASS 的语义、短时间窗和未完成最优整定的边界，避免过度承诺。
