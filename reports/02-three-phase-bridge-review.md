# 第 02 章三轮复核记录

## 工程复核

检查对象：PLECS 模型、Python 场景脚本、7 份逐点 CSV、64 组合真值表、汇总 CSV 和测试报告。

发现并修正：

1. 旧稿把状态级 MATLAB 图作为主证据，无法证明功率桥电压和绕组电流；已重建真实 PLECS 两电平桥实验。
2. 初版派生模型遗漏子系统输入端口映射，PLECS 明确报错；已补齐端口并重新加载验证。
3. 初版 PASS 阈值要求 2 ms 电流超过 20 A，与两相串联 R-L 实际响应不符；根据参数复算后改为只判定方向、悬空相、KCL、线电压和 10 A 有效建立阈值。
4. 确认 7/7 场景 PASS，64/64 门极组合有唯一分类。

## 视觉与写作复核

检查对象：PLECS 原生 Scope 截图、MATLAB 四层路径图、64 组合矩阵、正文图文顺序和表格。

发现并修正：

1. 删除旧的 `bridge_state_scenarios.png` 和 `bridge_state_fault_matrix.png`，避免读者误认它们是功率级仿真。
2. PLECS 原生截图明确保留窗口标题和工具栏，用于识别证据来源。
3. MATLAB 路径图按命令、线电压、相电流、七场景末值排列，避免先给汇总后讲电流路径。
4. 文件清单移到复现和核心解释之后。

## 读者理解复核

检查问题：读者能否说明六路门极、三值相命令、PLECS 变流器、线电压和绕组电流分别由谁产生。

发现并修正：

1. 开头先纠正“三值相命令等于六路门极”的误读。
2. 增加单桥臂四状态表和 `AH+BL -> [+1,-1,0]` 最小例子。
3. 增加两相串联 R-L 公式，用 14.8 A 近似值交叉检查 PLECS 的 14.6509 A。
4. 结尾只预告第 03 章机械角、电角和极对数，不列多章路线。
